import React from 'react';
import { NewsList } from '../../const';

const NewsAll = () => {
    return (
    <section className='searchAll'>
        <NewsList />
        </section>
    );
};

export default NewsAll;